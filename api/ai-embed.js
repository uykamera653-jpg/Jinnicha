const OpenAI=require('openai');
const {createClient}=require('@supabase/supabase-js');
module.exports=async function(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST only'});try{const key=process.env.OPENAI_API_KEY;if(!key)return res.status(503).json({error:'OPENAI_API_KEY sozlanmagan.'});const body=req.body||{};const text=String(body.text||'').trim();if(!text)return res.status(400).json({error:'Qidiruv matni kerak.'});const ai=new OpenAI({apiKey:key});const e=await ai.embeddings.create({model:'text-embedding-3-small',input:text});const embedding=e.data[0].embedding;const sb=createClient(process.env.SUPABASE_URL||'https://neavvgcxakefvlvecndc.supabase.co',process.env.SUPABASE_SERVICE_ROLE_KEY||process.env.SUPABASE_KEY);const {data,error}=await sb.rpc('findo_ai_matches',{query_embedding:embedding,match_count:30,min_similarity:Number(body.min_similarity||.25)});if(error)throw error;return res.status(200).json({query:text,matches:data||[]});}catch(e){console.error(e);return res.status(500).json({error:e.message||'AI qidiruv xatosi.'})}};
