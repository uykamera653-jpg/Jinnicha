(() => {
  const db = window.supabase.createClient('https://neavvgcxakefvlvecndc.supabase.co','sb_publishable_1o7-MHRPV_jKSs-xWA3odw_UhTNCuVM');
  const vid=()=>localStorage.getItem('findo_visitor_id')||'visitor_guest';
  const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));
  const items=()=>Array.isArray(window.items)?window.items:[];
  const toast=t=>window.toast?window.toast(t):alert(t);
  const close=()=>window.closeM?window.closeM():document.querySelectorAll('.modal').forEach(x=>x.classList.remove('show'));
  const opts=(found=true)=>items().filter(x=>found||x.type==='lost').map(x=>`<option value="${esc(x.id)}">${esc(x.title)} — ${esc(x.location)}</option>`).join('');
  const fmt=d=>d?new Date(d).toLocaleString('uz-UZ',{dateStyle:'medium',timeStyle:'short'}):'';
  async function put(table,row){const {error}=await db.from(table).insert(row);if(error){console.error(table,error);toast('❌ Maʼlumot saqlanmadi');return false}return true}
  async function list(table){const {data,error}=await db.from(table).select('*').eq('visitor_id',vid()).order('created_at',{ascending:false});if(error){console.error(table,error);return[]}return data||[]}
  async function aiMatch(text,candidates){try{const r=await fetch('/api/ai',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'match',text,items:candidates.slice(0,20)})});if(!r.ok)return null;const j=await r.json();return Array.isArray(j.matches)?j:null}catch{return null}}

  async function reward(){const m=document.getElementById('reward');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>💰 Mukofot</h2><button class="x" onclick="closeM()">×</button></div><p>Mukofotni real eʼlonga bog‘lab saqlang.</p><form id="rewardForm" class="form"><select id="rewardItem" required><option value="">Buyumni tanlang</option>${opts(false)}</select><input id="rewardAmount" required placeholder="500 000 so‘m"><textarea id="rewardNote" placeholder="Mukofot shartlari..."></textarea><button class="btn primary">💾 Mukofotni saqlash</button></form><div id="rewardList" class="list"></div></div>`;rewardForm.onsubmit=async e=>{e.preventDefault();if(await put('rewards',{visitor_id:vid(),item_id:rewardItem.value,amount:rewardAmount.value.trim(),note:rewardNote.value.trim()})){toast('✅ Mukofot saqlandi');renderRewards()}};renderRewards()}
  async function renderRewards(){const el=document.getElementById('rewardList');if(!el)return;const data=await list('rewards');el.innerHTML=data.filter(x=>x.status==='active').map(x=>`<div class="listItem">💰 <b>${esc(x.amount)}</b> — ${esc((items().find(i=>i.id===x.item_id)||{}).title||'Buyum')}<br><small>${esc(x.note||'')} · ${fmt(x.created_at)}</small></div>`).join('')||'<p>Hali mukofot yo‘q.</p>'}

  async function proof(){const m=document.getElementById('proof');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>🔐 Egani tasdiqlash</h2><button class="x" onclick="closeM()">×</button></div><p>Buyum egasi ekaningizni tasdiqlash uchun so‘rov yuboring.</p><form id="proofForm" class="form"><select id="proofItem" required><option value="">Buyumni tanlang</option>${opts()}</select><select id="proofType"><option>Buyum belgisi</option><option>Oxirgi joy</option><option>Sotib olish dalili</option><option>Boshqa</option></select><textarea id="proofAnswer" required placeholder="Faqat egasi biladigan maʼlumot..."></textarea><button class="btn primary">🔐 So‘rov yuborish</button></form><div id="proofList" class="list"></div></div>`;proofForm.onsubmit=async e=>{e.preventDefault();if(await put('owner_proofs',{visitor_id:vid(),item_id:proofItem.value,proof_type:proofType.value,answer:proofAnswer.value.trim()})){toast('✅ So‘rov yuborildi');renderProof()}};renderProof()}
  async function renderProof(){const el=document.getElementById('proofList');if(!el)return;const data=await list('owner_proofs');el.innerHTML=data.map(x=>`<div class="listItem">🔐 ${esc((items().find(i=>i.id===x.item_id)||{}).title||'Buyum')} · <b>${esc(x.status||'pending')}</b><br><small>${esc(x.proof_type)} · ${fmt(x.created_at)}</small></div>`).join('')||'<p>Tasdiqlash so‘rovlari yo‘q.</p>'}

  async function safety(){const m=document.getElementById('safety');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>🛡️ Xavfsizlik markazi</h2><button class="x" onclick="closeM()">×</button></div><p>Firibgar yoki shubhali eʼlonni moderatorga yuboring.</p><form id="reportForm" class="form"><select id="reportItem" required><option value="">Eʼlonni tanlang</option>${opts()}</select><select id="reportReason"><option>Firibgarlik</option><option>Noto‘g‘ri maʼlumot</option><option>Shubhali aloqa</option><option>Noqonuniy kontent</option><option>Boshqa</option></select><textarea id="reportDetails" placeholder="Muammoni yozing..."></textarea><button class="btn primary">🚨 Xabar yuborish</button></form><div id="reportList" class="list"></div></div>`;reportForm.onsubmit=async e=>{e.preventDefault();if(await put('reports',{visitor_id:vid(),item_id:reportItem.value,reason:reportReason.value,details:reportDetails.value.trim()})){toast('✅ Xabar yuborildi');renderReports()}};renderReports()}
  async function renderReports(){const el=document.getElementById('reportList');if(!el)return;const data=await list('reports');el.innerHTML=data.map(x=>`<div class="listItem">🚨 ${esc(x.reason)} · <b>${esc(x.status||'open')}</b><br><small>${fmt(x.created_at)}</small></div>`).join('')||'<p>Yuborilgan xabarlar yo‘q.</p>'}

  async function hero(){const m=document.getElementById('hero');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>🏆 Findo Hero</h2><button class="x" onclick="closeM()">×</button></div><p>Yordam berganingiz uchun ball yig‘ing.</p><div class="heroCard"><div id="heroScore" style="font-size:42px;font-weight:900">0</div><div>Findo Hero ball</div></div><div id="heroList" class="list"></div></div>`;const data=await list('hero_events');heroScore.textContent=data.reduce((s,x)=>s+Number(x.points||0),0);heroList.innerHTML=data.map(x=>`<div class="listItem">🏆 ${esc(x.action)} <b>+${x.points}</b> · ${fmt(x.created_at)}</div>`).join('')||'<p>Hali faoliyat yo‘q.</p>'}
  window.findoHeroEvent=(action,item_id,points=1)=>put('hero_events',{visitor_id:vid(),action,item_id:item_id||null,points});

  async function business(){const m=document.getElementById('business');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>🏢 Tashkilot rejimi</h2><button class="x" onclick="closeM()">×</button></div><p>Hotel, savdo markazi, taksi, aeroport yoki maktab uchun profil yarating.</p><form id="bizForm" class="form"><input id="bizName" required placeholder="Tashkilot nomi"><select id="bizType"><option>Hotel</option><option>Savdo markazi</option><option>Taksi</option><option>Aeroport</option><option>Maktab</option><option>Boshqa</option></select><input id="bizLocation" required placeholder="Shahar / manzil"><input id="bizContact" placeholder="Telefon / elektron pochta"><button class="btn primary">🏢 Saqlash</button></form><div id="bizList" class="list"></div></div>`;bizForm.onsubmit=async e=>{e.preventDefault();if(await put('businesses',{visitor_id:vid(),name:bizName.value.trim(),business_type:bizType.value,location:bizLocation.value.trim(),contact:bizContact.value.trim()})){toast('✅ Tashkilot yaratildi');renderBusinesses()}};renderBusinesses()}
  async function renderBusinesses(){const el=document.getElementById('bizList');if(!el)return;const data=await list('businesses');el.innerHTML=data.map(x=>`<div class="listItem">🏢 <b>${esc(x.name)}</b><br>${esc(x.business_type)} · ${esc(x.location)}<br>${esc(x.contact||'')} · ${fmt(x.created_at)}</div>`).join('')||'<p>Hali tashkilot yo‘q.</p>'}

  const dist=(a,b,c,d)=>{const R=6371,r=x=>x*Math.PI/180,da=r(c-a),db=r(d-b),h=Math.sin(da/2)**2+Math.cos(r(a))*Math.cos(r(c))*Math.sin(db/2)**2;return R*2*Math.atan2(Math.sqrt(h),Math.sqrt(1-h))};
  const words=s=>String(s||'').toLowerCase().replace(/[^\p{L}\p{N}]+/gu,' ').split(/\s+/).filter(x=>x.length>2);
  const score=(q,x)=>{const a=[...new Set(words(q))],b=words([x.title,x.category,x.location,x.description,x.ai_brand_model,(x.ai_colors||[]).join(' ')].join(' '));if(!a.length)return 0;return Math.min(99,Math.round(a.filter(w=>b.some(t=>t.includes(w)||w.includes(t))).length/a.length*100))};

  async function detective(){const m=document.getElementById('detective');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>🕵️ Oxirgi ko‘rilgan joy tahlili</h2><button class="x" onclick="closeM()">×</button></div><p>Yo‘nalish, GPS va Findo bazasidagi topilgan eʼlonlarni birga tahlil qiladi.</p><form id="detForm" class="form"><select id="detItem"><option value="">Buyumni tanlang</option>${opts(false)}</select><input id="d1x" required placeholder="Oxirgi marta qayerda ko‘rdingiz?"><input id="d2x" placeholder="Keyin qayerga bordingiz?"><input id="d3x" placeholder="Taksi, avtobus, piyoda..."><div class="row"><input id="detLat" type="number" step="any" placeholder="GPS kenglik"><input id="detLon" type="number" step="any" placeholder="GPS uzunlik"></div><button type="button" id="detGps" class="btn">📍 Hozirgi joylashuvim</button><button class="btn primary">🕵️ Tahlil qilish va saqlash</button></form><div id="detOut" class="list"></div></div>`;detGps.onclick=()=>navigator.geolocation?.getCurrentPosition(p=>{detLat.value=p.coords.latitude;detLon.value=p.coords.longitude;toast('📍 GPS olindi')},()=>toast('GPS ruxsati berilmadi'));detForm.onsubmit=async e=>{e.preventDefault();const item=items().find(x=>x.id===detItem.value)||{};const q=[item.title,item.category,item.description,d1x.value,d2x.value,d3x.value].join(' ');let cs=items().filter(x=>x.type==='found'&&x.status!=='archived').map(x=>({...x,score:score(q,x)})).sort((a,b)=>b.score-a.score).slice(0,5);const ai=await aiMatch(q,cs);if(ai?.matches?.length){const map=new Map(ai.matches.map(x=>[x.id,x]));cs.forEach(x=>{const z=map.get(x.id);if(z){x.score=Math.max(x.score,Number(z.score)||0);x.reason=z.reason||''}});cs.sort((a,b)=>b.score-a.score)}const best=cs[0];const route=[d1x.value.trim(),d2x.value.trim()].filter(Boolean).join(' → ');const confidence=best?.score>=70?'Yuqori':best?.score>=40?'O‘rta':'Past';const result=`Yo‘nalish: ${route||'ko‘rsatilmagan'}${d3x.value.trim()?' · transport: '+d3x.value.trim():''}. Eng mos eʼlon: ${best?.title||'topilmadi'}. Moslik: ${best?.score||0}%. Ishonchlilik: ${confidence}.`;if(await put('detective_cases',{visitor_id:vid(),item_id:detItem.value||null,last_seen:d1x.value.trim(),next_place:d2x.value.trim(),transport:d3x.value.trim(),result,latitude:detLat.value?Number(detLat.value):null,longitude:detLon.value?Number(detLon.value):null})){detOut.innerHTML=`<div class="match"><b>✅ Tahlil tayyor</b><p>${esc(result)}</p><small>${ai?'AI + baza mosligi':'Baza mosligi'}</small></div>`+cs.map(x=>`<div class="listItem">🟢 <b>${esc(x.title)}</b> — ${x.score}%<br><small>${esc(x.location||'')} ${x.reason?'· '+esc(x.reason):''}</small></div>`).join('');toast('✅ Detective saqlandi')}}}

  async function qr(){const m=document.getElementById('qr');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>🔗 Findo ID + QR</h2><button class="x" onclick="closeM()">×</button></div><p>Buyum uchun unikal QR kod yarating.</p><form id="qrForm" class="form"><select id="qrItem" required><option value="">Buyumni tanlang</option>${opts()}</select><input id="qrLabel" placeholder="Masalan: Uy kaliti"><button class="btn primary">🔗 QR yaratish</button></form><div id="qrResult" style="margin-top:15px;text-align:center"></div><div id="qrList" class="list"></div></div>`;qrForm.onsubmit=async e=>{e.preventDefault();const code='FN-'+crypto.randomUUID().replaceAll('-','').slice(0,12).toUpperCase();if(await put('qr_items',{visitor_id:vid(),item_id:qrItem.value,code,label:qrLabel.value.trim()})){const url=location.origin+location.pathname+'?qr='+encodeURIComponent(code);renderQR(url,code);renderQRList();toast('✅ QR yaratildi')}};renderQRList()}
  async function renderQR(url,code){const el=document.getElementById('qrResult');if(!el)return;el.innerHTML='<div id="qrCanvas"></div><b>'+esc(code)+'</b><p><small>QR havolasi tayyor</small></p>';if(!window.QRCode)await new Promise((res,rej)=>{const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js';s.onload=res;s.onerror=rej;document.head.appendChild(s)}).catch(()=>{});if(window.QRCode)new QRCode(document.getElementById('qrCanvas'),{text:url,width:190,height:190})}
  async function renderQRList(){const el=document.getElementById('qrList');if(!el)return;const data=await list('qr_items');el.innerHTML=data.map(x=>`<div class="listItem">🔗 <b>${esc(x.code)}</b> · ${esc(x.label||'')}<br><small>${fmt(x.created_at)}</small></div>`).join('')||'<p>Hali QR yo‘q.</p>'}

  async function nearby(){const m=document.getElementById('nearby');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>📍 Yaqindagi eʼlonlar</h2><button class="x" onclick="closeM()">×</button></div><div class="alert">GPS orqali real eʼlonlarni masofa bo‘yicha saralaymiz.</div><div class="row"><input id="nearRadius" type="number" min="1" max="100" value="10" placeholder="Radius (km)"><button id="nearBtn" class="btn primary">📍 Joylashuvim</button></div><div id="nearbyList" class="list"></div></div>`;nearBtn.onclick=()=>navigator.geolocation?.getCurrentPosition(p=>{const r=Math.min(100,Math.max(1,Number(nearRadius.value)||10));const rows=items().map(x=>({...x,dist:x.latitude!=null&&x.longitude!=null?dist(p.coords.latitude,p.coords.longitude,x.latitude,x.longitude):null})).filter(x=>x.dist!=null&&x.dist<=r).sort((a,b)=>a.dist-b.dist);nearbyList.innerHTML=rows.map(x=>`<div class="listItem">${x.type==='found'?'🟢':'🔴'} <b>${esc(x.title)}</b><br>${esc(x.location||'')} · <b>${x.dist.toFixed(1)} km</b></div>`).join('')||'<p>Bu radiusda GPS koordinatasi bor eʼlon topilmadi.</p>'},()=>toast('GPS ruxsati berilmadi'))}

  async function visual(){const m=document.getElementById('visual');if(!m)return;m.innerHTML=`<div class="box"><div class="head"><h2>📸 Rasm orqali qidirish</h2><button class="x" onclick="closeM()">×</button></div><p>Rasmni Findo Storage’ga xavfsiz yuklaymiz, qidiruvni saqlaymiz va rasmli eʼlonlarni ko‘rsatamiz.</p><input id="visualFile" type="file" accept="image/*"><img id="visualPrev2" style="display:none;max-width:100%;margin-top:12px;border-radius:12px"><div id="visualOut2" class="list"></div></div>`;visualFile.onchange=async e=>{const f=e.target.files[0];if(!f)return;if(f.size>5*1024*1024)return toast('❌ Rasm 5 MB dan kichik bo‘lsin');const r=new FileReader();r.onload=()=>{visualPrev2.src=r.result;visualPrev2.style.display='block'};r.readAsDataURL(f);visualOut2.innerHTML='<div class="match">⏳ Rasm yuklanmoqda...</div>';const b=await f.arrayBuffer(),h=await crypto.subtle.digest('SHA-256',b),hash=[...new Uint8Array(h)].map(x=>x.toString(16).padStart(2,'0')).join('').slice(0,24);let url='';try{const path=`${vid()}/${Date.now()}-${f.name.replace(/[^a-zA-Z0-9._-]/g,'_')}`;const up=await db.storage.from('item-images').upload(path,f,{contentType:f.type,upsert:false});if(!up.error)url=db.storage.from('item-images').getPublicUrl(path).data.publicUrl}catch(err){console.warn(err)}await put('visual_searches',{visitor_id:vid(),image_hash:hash,image_url:url||null,query_text:f.name});const cs=items().filter(x=>x.image_path||x.image_url).slice(0,20);visualOut2.innerHTML=`<div class="match"><b>✅ Rasm qabul qilindi</b><p>${cs.length} ta rasmli eʼlon mavjud.</p><small>Qidiruv ID: ${esc(hash)}</small></div>`+cs.map(x=>`<div class="listItem">📷 <b>${esc(x.title)}</b><br>${esc(x.location||'')}</div>`).join('')};}

  const originalOpen=window.openM;
  window.openM=async id=>{if(originalOpen)originalOpen(id);try{if(id==='reward')await reward();else if(id==='proof')await proof();else if(id==='safety')await safety();else if(id==='hero')await hero();else if(id==='business')await business();else if(id==='detective')await detective();else if(id==='qr')await qr();else if(id==='nearby')await nearby();else if(id==='visual')await visual()}catch(e){console.error('Findo feature error',id,e);toast('❌ Bo‘limni ochishda xatolik yuz berdi')}};
})();
